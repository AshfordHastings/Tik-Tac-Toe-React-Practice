// reducer is passed to the store, which updates the state by passing the state and action to the reducer

// import the different actions 

// define the initial state
const initialState = {

}

function reducer(state, action) {

}

export default reducer;