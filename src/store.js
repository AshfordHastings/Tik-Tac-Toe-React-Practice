// create the store and pass in the reducer with the initial state defined

import { createStore } from 'redux';
import reducer from './reducer';



const store = redux.createStore(reducer);

export default store;